# name: TelegramBot

# on:
#   pull_request:
#     branches: [ devel ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@master
#       - name: send custom message with args
#         uses: appleboy/telegram-action@master
#         with:
#           to: "-groupid"
#           token: "token here"
#           args: 👨🏻‍💻 Usuário ${{github.actor}}, fez um ${{ github.event_name }}. no repositório ${{ github.repository }} | Função ${{github.job}}.
