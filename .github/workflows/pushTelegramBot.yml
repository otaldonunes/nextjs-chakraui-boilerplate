# name: TelegramBot

# on:
#   push:
#     branches: [ main, devel ]

# jobs:
#   Merge:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@master
#       - name: send custom message with args
#         uses: appleboy/telegram-action@master
#         with:
#           to: "-groupid"
#           token: "token here"
#           args: 👨🏻‍💻 Usuário ${{github.actor}}, fez um MERGE no repositório ${{ github.repository }} | Atualizar sua branch!!!.
